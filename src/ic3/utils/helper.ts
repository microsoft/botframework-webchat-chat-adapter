export const activityMap = new Map<string, any>(); //add clean logic when go over the size

export function isEchoBackMessage(message: Microsoft.CRM.Omnichannel.IC3Client.Model.IMessage): boolean{
    return false;
}